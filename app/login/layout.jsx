export const metadata = {
  title: 'Log in',
  description: 'Log in to Chess Arena to play live chess, join tournaments, and track your Elo rating.',
  robots: { index: false, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
