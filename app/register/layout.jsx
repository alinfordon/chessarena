export const metadata = {
  title: 'Create account',
  description: 'Create a free Chess Arena account and start playing live chess online.',
};

export default function RegisterLayout({ children }) {
  return children;
}
